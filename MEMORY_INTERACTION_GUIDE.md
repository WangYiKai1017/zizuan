# Memory Manager 交互式测试指南

## 功能介绍

这个交互式测试脚本允许您模拟采访过程，输入采访内容后，系统会自动：

1. 使用 LLM 将原始对话内容提炼为结构化记忆
2. 识别并提取事件、人物和时间线信息
3. 更新人物画像记忆
4. 将所有信息以 Markdown 格式存储在文件系统中

## 快速开始

### 运行脚本

在项目根目录下执行：

```bash
python test_memory_interaction.py
```

### 使用流程

1. **初始化阶段**：
   - 系统会自动创建一个临时目录用于存储记忆文件
   - 显示临时目录路径（可在测试结束后查看生成的 Markdown 文件）

2. **选择人生阶段**：
   ```
   请选择当前采访的人生阶段:
   1. 童年时期
   2. 少年时期
   3. 中年时期
   4. 老年时期
   输入选项 (1-4): 
   ```

3. **开始采访**：
   - 输入用户的采访回答
   - 系统会实时处理并显示结果

4. **特殊命令**：
   - `exit` - 结束采访
   - `history` - 查看历史对话记录
   - `clear` - 清空历史记录和会话状态

## 输出说明

处理完成后，系统会显示以下信息：

### 1. 提取的事件

```
📅 提取的事件 (2个):
   - 童年小院生活 (童年时期)
     用户童年住在有枣树的小院子里...
   - 与父亲的互动 (童年时期)
     父亲经常在院子里教用户读书写字...
```

### 2. 提取的人物

```
👤 提取的人物 (1个):
   - 父亲 (父亲)
     用户的父亲，经常在院子里教用户读书写字...
```

### 3. 人物画像更新

```
👤 人物画像更新:
   - 出生年份: 1950年
   - 出生地: 农村
   - 性格特点: 怀旧, 热爱学习...
```

### 4. 存储位置

```
📁 所有内容已保存到:
   - 时间线: C:\Users\MSI\AppData\Local\Temp\memory_test_hh64p6sv\timeline\life-events.md
   - 事件: C:\Users\MSI\AppData\Local\Temp\memory_test_hh64p6sv\events
   - 人物: C:\Users\MSI\AppData\Local\Temp\memory_test_hh64p6sv\people
```

## 文件结构说明

生成的 Markdown 文件按以下结构组织：

```
memory_test_xxx/
├── timeline/
│   └── life-events.md        # 主时间线文件
├── events/
│   ├── childhood/            # 童年时期事件
│   │   ├── 童年小院生活.md
│   │   └── 与父亲的互动.md
│   └── ...                   # 其他人生阶段
└── people/
    ├── family/               # 家庭成员
    │   └── 父亲.md
    └── ...                   # 其他人物类别
```

## 示例输入输出

### 输入示例

```
用户: 我记得小时候住在一个小院子里，院子里种了一棵很大的枣树。
```

### 输出示例

```
🔄 正在处理采访内容...

✅ 内容处理完成!

📅 提取的事件 (1个):
   - 童年小院生活 (童年时期)
     用户童年住在有枣树的小院子里

👤 提取的人物 (0个):

👤 人物画像更新:
   - 出生年份: 未知
   - 出生地: 农村（推测）
   - 性格特点: 怀旧

📁 所有内容已保存到:
   - 时间线: C:\Users\MSI\AppData\Local\Temp\memory_test_hh64p6sv\timeline\life-events.md
   - 事件: C:\Users\MSI\AppData\Local\Temp\memory_test_hh64p6sv\events
   - 人物: C:\Users\MSI\AppData\Local\Temp\memory_test_hh64p6sv\people
```

## 技术实现

该脚本使用了以下核心组件：

1. **MemoryManager** - 协调记忆的组织和存储
2. **MemoryRepository** - 负责三层记忆的实际存储
3. **MarkdownFileManager** - 处理文件系统操作
4. **LLMService** - 调用大模型进行内容理解和结构化

## 注意事项

1. 确保已正确配置 `.env` 文件中的大模型参数
2. 第一次运行可能需要加载模型，会稍慢一些
3. 临时目录在测试结束后会保留，您可以手动查看和管理
4. 建议每次采访聚焦于一个主题，获得更好的结构化结果

## 故障排除

### 连接大模型失败
- 检查 `.env` 文件中的 API 密钥和端点配置
- 确保网络连接正常

### 输出乱码
- 检查终端编码设置（建议使用 UTF-8）

### 处理速度慢
- 这可能是由于大模型响应速度导致的正常现象
- 建议每次输入不要过长的内容

## 后续开发建议

1. 添加更复杂的对话管理功能
2. 支持批量导入已有采访记录
3. 实现记忆的可视化展示
4. 支持多用户和多会话管理