- Agent 节点方法统一接收当前状态对象并返回字典格式的增量更新，由 LangGraph 自动合并至全局状态。
- 所有 LLM 交互均通过 `LLMService.invoke_with_template` 执行，利用外部 Markdown 模板解耦提示词与业务代码。
- 服务层接口（如 `BiographyFileManager`）优先使用相对路径操作，并在内部自动处理与知识库根路径的拼接。
- 异步 Runner 类继承自 `BaseAgentRunner`，通过注入的 `SSEEmitter` 统一向外发送标准化的进度事件。