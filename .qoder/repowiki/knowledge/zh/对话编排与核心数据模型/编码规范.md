- 所有数据模型类均继承自 `pydantic.BaseModel` 并使用 `Field` 进行元数据描述和默认值设定。
- 枚举类型统一继承 `str, Enum` 以确保序列化后的可读性及类型安全。
- 核心状态类（如 `SessionState`, `KBOrganizerState`）内部封装了状态变更方法（如 `add_turn`, `register_merge`）以维护业务规则。
- 数据模型文件中普遍包含详细的职责（职责）和使用场景（使用场景）文档字符串，以明确其在系统中的定位。