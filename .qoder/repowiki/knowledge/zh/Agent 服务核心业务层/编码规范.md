- 所有涉及 user_id 的请求模型均通过 Pydantic `field_validator` 执行统一的正则表达式校验（3-50位字母数字及下划线）。
- 文件服务接口采用嵌套字典结构返回错误详情（如 `{"error": {"code": "...", "message": "..."}}`），以保持 API 响应格式的一致性。
- 会话管理与 SSE 发射器均采用异步队列或锁机制（`asyncio.Lock` / `asyncio.Queue`）来确保高并发下的线程安全与事件有序性。