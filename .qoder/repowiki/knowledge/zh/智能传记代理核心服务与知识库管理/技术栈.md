- **LLM框架**：使用 `langchain` 及其扩展（`langchain_openai`, `langchain_anthropic`）构建 Agent 和工具链。
- **可观测性**：集成 `langfuse` 进行 LLM 调用的追踪与监控。
- **异步I/O**：采用 `aiofiles` 处理 Markdown 文件的异步读写，提升高并发下的 I/O 性能。
- **配置与序列化**：使用 `pydantic` 进行数据模型校验，`yaml` 管理传记大纲配置。