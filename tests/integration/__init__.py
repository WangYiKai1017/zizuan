"""Standalone HTTP/SSE integration tests for the Agent FastAPI service.

These scripts are NOT pytest tests. They are runnable directly with python3
and assume a live service is running at SERVICE_URL (default
http://localhost:8000).
"""
